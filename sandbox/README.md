# Sandbox — eval targets

Application code for **real** poteto/ponytail tests lives here. Harness meta stays in `.cursor/`.

| Folder | Role |
|--------|------|
| `fixture-app/` | Auth bug + utils refactor targets (tests #5, #7) |
| `run-02/` | Prior harness test #2 (email CLI) |

Start here for `fix auth`, `refactor utils`, or any prompt that needs code to touch.
